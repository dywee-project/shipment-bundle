{{ form_start(form) }}

{{ form_rest(form) }}

<div class="form-group">
    <div class="col-sm-2"></div>
    <div class="col-sm-10">
        <button type="submit" class="btn-success btn-block btn">
            <i class="fa fa-check"></i> {{ 'validate'|trans }}
        </button>
    </div>
</div>

{{ form_end(form) }}