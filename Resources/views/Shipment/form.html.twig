{{ form_start(form) }}
{{ form_rest(form) }}

<div class="col-sm-9 col-sm-offset-2">
    <button type="submit" class="btn btn-success btn-block"><i class="check"></i> Valider</button>
</div>

{{ form_end(form) }}